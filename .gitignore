
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Agent tooling and local issue tracking: useful on a working copy, not part of
# the library. .beads carries a 6MB Dolt database of issue history -- kept on
# disk, just not published here.
.beads/
.agents/
.claude/
.codex/

# jolt's per-project dependency cache
.jolt/cpcache/

# Agent instruction files: kept on a working copy, not published here.
AGENTS.md
CLAUDE.md

# Generated documentation site (bb build, from a docs-engine checkout).
# Published by CI from main; never committed, since a stale build in the
# tree is worse than none.
_site/
